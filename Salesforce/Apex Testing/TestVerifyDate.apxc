@isTest
private class TestVerifyDate {
    static testMethod void TestVerifyDate() {
      VerifyDate.CheckDates(System.today(),System.today().addDays(5));
       VerifyDate.CheckDates(System.today(),System.today().addDays(81));
    }
}